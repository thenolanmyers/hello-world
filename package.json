{
  "name": "hello-world",
  "version": "1.0.0",
  "description": "Test sandbox for new skills",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "babel src -d lib"
  },
  "author": "nsm",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.6.1"
  },
  "dependencies": {
    "latest-version": "^3.1.0"
  }
}
